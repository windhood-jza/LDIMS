/**
 * @interface NameValueData
 * @description 通用的名称-值对数据结构，用于图表
 * @property {string} name - 名称 (例如部门名称、类型名称)
 * @property {number} value - 数量值
 */
export interface NameValueData {
  name: string;
  value: number;
} 